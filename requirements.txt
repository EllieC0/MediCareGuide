streamlit>=1.35
pandas
zipcodes
requests

# RAG pipeline (required — WELCOME-mode handbook Q&A)
pypdf
fastembed
faiss-cpu

# Optional — voice output (Text-to-Speech)
# kokoro-onnx
# onnxruntime
# soundfile
# sounddevice

# Optional — voice input (Speech-to-Text)
# faster-whisper
# sounddevice
# ffmpeg  ← system package, not pip: brew install ffmpeg  (needed for WebM→WAV conversion)
